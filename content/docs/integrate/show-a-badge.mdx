---
title: Show a Trust Score badge
description: Render a wallet's verification state without gating any content.
---

`EntrosBadge` is a read-only display. Given a wallet address, it shows the verification state and Trust Score. No gating, no redirects.

## Get the component

`EntrosBadge` is a single-file React component you copy into your project from the entros.io repo: [`src/components/ui/entros-badge.tsx`](https://github.com/entros-protocol/entros.io/blob/main/src/components/ui/entros-badge.tsx). It will be packaged as a standalone npm component as part of the [near-term roadmap](/docs/roadmap/near-term).

## Usage

```tsx
import { EntrosBadge } from "@/components/ui/entros-badge";

<EntrosBadge walletAddress={comment.author} />
```

The component fetches the Anchor PDA on mount and renders one of three states:

- **Verified**—shows the Trust Score and last-verified timestamp
- **Not verified**—shows a neutral "no Anchor" indicator
- **Loading**—shows a placeholder with the same dimensions as the eventual content

## Props

| Prop | Type | Required | Default | Purpose |
| --- | --- | --- | --- | --- |
| `walletAddress` | `string` | yes |—| Base58-encoded wallet pubkey to look up |
| `connection` | `Connection` | no | devnet RPC | Solana connection. Provide your own to read mainnet or use a different RPC. |
| `className` | `string` | no |—| Tailwind classes appended to the root element |

## Where badges work well

- **Author bylines.** A small badge next to a comment author signals the commenter's verification level without forcing the reader to dig into a profile.
- **Leaderboards and lists.** Sorting or coloring rows by score gives an at-a-glance read on participant quality.
- **Onboarding flows.** A user progressing through verification can see their score climb without having to look up the Anchor PDA themselves.

## Server-rendering badges

`EntrosBadge` is a client component. To render a badge as static content, read the score on the server and pass it down:

```tsx
const score = await readTrustScore(connection, walletPubkey);
return <StaticBadge score={score} />;
```

The PDA layout is documented in the [read-on-chain integration guide](/docs/integrate/read-on-chain).

## Where to look next

- [Gate a route](/docs/integrate/gate-a-route)—when you want to block, not just display
- [Read on-chain](/docs/integrate/read-on-chain)—the underlying RPC pattern
